# Drug Combination Synergy Predictor — Backend Dependencies
# Install with: pip install -r requirements.txt
# For RDKit, use conda: conda install -c conda-forge rdkit

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0
pydantic>=2.0.0
python-multipart>=0.0.9

# RDKit — install separately via conda:
# conda install -c conda-forge rdkit

# Optional advanced models
xgboost>=2.0.0
